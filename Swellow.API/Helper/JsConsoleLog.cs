using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace Swellow.API.Helper
{
    public class JsConsoleLog
    {
        //Console.WriteLine("FolderPicker Render完毕！");
        //// 获取所有属性。
        //Type type = typeof(DirectoryDetail);
        //PropertyInfo[] properties = type.GetProperties();
        //// 遍历属性打印到控制台。
        //foreach (PropertyInfo prop in properties)
        //{
        //    Console.WriteLine(type.GetProperty(prop.ToString()).GetValue(Directory, null));
        //}
    }
}
